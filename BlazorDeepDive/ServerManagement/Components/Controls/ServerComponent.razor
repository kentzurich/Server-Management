<p>
    @Server.Name is in @Server.City
</p>

<div data-name="@Server.Name">
    @Server.Name is in @Server.City
</div>

@code {
    private Server Server = new() { Name = "Server 1", City = "Imus, Cavite" };
}
